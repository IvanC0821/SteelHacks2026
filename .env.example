# Read these explicitly in your shell or deployment runner; no dotenv auto-loading.
VERITY_DATA_DIR=.data
VERITY_CORS_ORIGINS=http://localhost:3000,http://localhost:5173
# Optional fictional fixture provider. Leave unset for the normal no-model backend.
# VERITY_PROVIDER_FACTORY=scripts.demo_data:create_provider
