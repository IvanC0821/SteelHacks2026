# Task 7: QA harness (API client tests and end-to-end smoke)

Owns `frontend/src/api/*.test.ts`, `frontend/e2e/**`, and `frontend/tools/reseed.sh`. Read `design/BRIEF.md` and `docs/FRONTEND.md` first.

## Part A: client unit tests (start now, no UI needed)

Vitest tests for `src/api/client.ts` with a mocked `fetch`: bearer header on every call, JSON vs multipart bodies, `ApiError` mapping for 401/403/409/422 (`detail.code`, `detail.fields`), string `detail`, network failure → code `network`, `waitForJob` polling until `succeeded`/`failed` with growing delay (use fake timers), `documentBlob` never puts the token in the URL. Also `src/api/session.test.ts` if Task 1 has not written one (check first; do not duplicate).

## Part B: reseed script

`frontend/tools/reseed.sh`: kills nothing; checks the backend health; runs the seed from the repo root with the right `PYTHONPATH` and venv; prints the identities. Document it in `frontend/README.md` (rewrite that file: it is the Vite template; make it the frontend's own README: run, seed, identities, screenshots, tests, structure, ownership table from the brief).

## Part C: end-to-end smoke (after `design/DESIGN.md` says `Status: ready for Tasks 2–5`, and grows as pages land)

Playwright test runner is not installed; use plain `playwright` scripts under `frontend/e2e/*.mjs` runnable with `node`, each exiting non-zero on failure, plus `frontend/e2e/run.mjs` that runs them all and prints a table. Inject identities the way `tools/shot.mjs` does (read `.dev/session.json`, `addInitScript` sets `localStorage["verity.session"]`). Start with:

1. `session.mjs`: no session → `/session` renders; paste Sam Reyes's token → lands on the course; sign out clears storage.
2. `guards.mjs`: a student visiting `/a/:id/grade` gets the one-sentence denied state, no crash, no console errors; a TA visiting a student-only route likewise.
3. `console.mjs`: visit every route in the brief's ownership table as the right identity and fail on any console error or failed network request other than expected 4xx.
4. Then one flow per page task as they appear on disk (poll `src/pages/*/routes.tsx` for real routes): grading save-and-next persists and re-renders after reload; rubric publish gate reasons; student upload → assign pages → check → feedback (use a generated PDF from `tools/seed_dev.py`'s writer via a small Python call, or a fixture from `tools/fixtures/out` if present); overview renders bars with the seeded analytics.

Keep each script under 120 lines, deterministic, and independent of screenshot pixels (assert on text, roles, URLs, and network). Report which scripts pass, which routes are not yet implemented (skip, do not fail), and any bug you find in another task's page with the exact repro; do not fix other tasks' files.
